# 今天吃啥 AI 版 · 项目开发规范

> 基于《Vibe Coding 小程序全栈开发规范》适配 H5 场景

## 技术栈

- **前端**：纯 H5（HTML5 + CSS3 + Vanilla JS），无框架依赖
- **适配**：移动端优先，支持小程序 web-view 嵌入
- **样式单位**：px（H5场景），设计 Token 从 CSS 变量取值
- **数据存储**：localStorage（前端本地存储）
- **后端**：规划中（预留 api/ 接口层，便于后续接入）

## 目录结构

```
├── index.html              # 主入口（单页应用，所有视图在一个HTML中）
├── css/
│   ├── style.css           # 主样式（含设计 Token、组件样式）
│   └── mini-program-adapter.css  # 小程序 web-view 适配样式
├── js/
│   ├── app.js              # 主逻辑（视图切换、事件绑定、状态管理）
│   ├── engine.js           # 推荐引擎（AI推算算法、评分系统）
│   ├── llm.js              # LLM 接口封装（注意：密钥需后端代理）
│   ├── moments.js          # 菜友圈/社交功能
│   ├── social.js           # 社交/通讯录功能
│   ├── mini-program-bridge.js  # 小程序桥接层
│   ├── data/
│   │   └── china.js        # 中国城市数据
│   ├── recipes.js          # 菜谱数据（在家做）
│   ├── recipes2.js         # 菜谱数据扩展
│   ├── recipes3.js         # 菜谱数据扩展
│   ├── recipes4.js         # 菜谱数据扩展
│   ├── restaurants.js      # 餐厅数据（出去吃）
│   └── takeout.js          # 外卖商家数据
├── api/                    # 【预留】接口定义层（后端接入时使用）
├── utils/                  # 【预留】工具函数
├── assets/
│   └── icons/              # 图标资源
├── dev_server.py           # 本地开发服务器
└── AGENTS.md               # 本文件
```

## 前端规范

### 设计 Token（CSS 变量）

所有视觉属性必须从 `:root` 定义的 CSS 变量取值，**禁止硬编码**：

```css
:root {
  /* 颜色 */
  --bg: #FAF6F0;           /* 页面背景 */
  --card: #FFFFFF;          /* 卡片背景 */
  --ink: #201E1A;           /* 主文字 */
  --ink-soft: #55514B;      /* 次要文字 */
  --muted: #8C857C;          /* 辅助文字 */
  --line: #EDE6DB;           /* 边框线 */
  --accent: #E14D2A;         /* 主题色（番茄红） */
  --accent-deep: #C63D1E;    /* 主题色深 */
  --accent-soft: #FBEBE3;     /* 主题色浅 */

  /* 圆角 */
  --radius: 22px;             /* 卡片圆角 */
  --radius-sm: 12px;          /* 小圆角 */

  /* 阴影 */
  --shadow: 0 10px 30px rgba(32, 30, 26, 0.06);
  --shadow-lg: 0 18px 50px rgba(32, 30, 26, 0.10);
}
```

### 组件复用规则

- UI 相似度较高 + 出现超过 2 次 → 必须封装为可复用的 CSS 类或 JS 渲染函数
- 优先使用已有的组件类：`.panel`、`.menu-card`、`.btn-primary`、`.chip`、`.field`
- 禁止复制粘贴大段 HTML 结构

### 样式规范

- 颜色、字号、间距、圆角从 CSS 变量取值
- 毛玻璃效果统一使用：`background: rgba(255,255,255,0.7); backdrop-filter: blur(16px);`
- 渐变朦胧效果统一使用：`linear-gradient(135deg, rgba(255,240,230,0.85), rgba(255,250,245,0.9))`

## 后端规范（规划中）

### 绝对不能放在前端的内容

- ❌ API Key、Token、数据库连接信息
- ❌ 支付逻辑、金额计算
- ❌ 权限判断、登录态校验
- ❌ 数据库写入操作
- ❌ 敏感数据处理

### 当前风险点

- `js/llm.js` 中 API Key 目前从 localStorage 读取并在前端使用 → **需后端代理**
- 所有数据都是前端假数据 → **需后端接口替换**

### 接口设计规范（后端接入时遵守）

- 统一返回格式：`{ code: 0, message: "success", data: {} }`
- 每个接口必须有：参数校验 + 权限验证 + 错误处理 + 操作日志
- 用户数据按用户ID隔离，只能操作自己的数据
- 删除操作用软删除，记录操作日志

## 开发流程

1. **先做前端页面**（假数据），确认流程和字段
2. **基于页面推导后端需求**：数据模型、接口清单、权限设计
3. **接口先行**：后端先定义接口文档，前端按接口文档对接
4. **联调验证**：每个接口测试正常和异常两种情况
5. **安全检查**：密钥不暴露、权限校验、参数校验

## 主题切换

支持多主题（番茄红/橙色/金色/绿色），通过 `body[data-theme="xxx"]` 切换 CSS 变量。

## 版本记录

- v4.1.0：当前版本，H5 单页应用，纯前端实现
- 后续：接入后端，完善数据持久化和安全机制
